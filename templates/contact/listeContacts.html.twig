{% extends 'base.html.twig' %}

{% block title %}Liste des contacts{% endblock %}

{% block body %}
<h1 class="text-center pt-3 pb-5">Liste des Contacts</h1>

<div class="row">
    {% for leContact in lesPages %}
    <div class="col-sm-12 col-md-12 col-lg-3">
        {{include('contact/_ficheContact.html.twig', {'mode':'listeContacts'})}}
    {% endfor %}
    </div>
    <div class="navigation col-md-6 d-flex align-items-center mx-auto mt-3">
        {{ knp_pagination_render(lesPages) }}
    </div>
</div>
{% endblock %}
